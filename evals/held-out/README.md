Held-out cases and tests are loaded only through explicit protected partition selection.
Keep this directory outside model-visible Task Workspaces. The `secret_word` sample
shows the protected manifest and test layout; operators can supply a separate
private root with the same structure.
